package datos;

// Importa la clase Celular desde el paquete negocio
import negocio.Celular;

// Clase para gestionar el inventario de celulares
public class InventarioCelulares {
    public boolean verificarDisponibilidad(Celular celular) {
        // Verificar disponibilidad del celular en el inventario
        return true; // Supongamos que siempre hay disponibilidad.
    }
}
